package AIs;
/**
 * verteilen->
 */

import gamecomponents.Level;
import gamecomponents.Mole;

import java.util.ArrayList;

class SetMoleAI {
    int[] cardValues = new int[]{0, 0, 0, 0, 0, 0};
    public static boolean setMole(Level level, ArrayList<Mole> moles){
        calculateNextPosition(level);
     return true;
    }

    private static void calculateNextPosition(Level level) {

    }


}
